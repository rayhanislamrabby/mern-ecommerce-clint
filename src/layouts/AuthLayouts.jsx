import React from 'react';
import Login from '../pages/Authentication/Login';

const AuthLayouts = () => {
    return (
        <div>
            <Login></Login>
        </div>
    );
};

export default AuthLayouts;